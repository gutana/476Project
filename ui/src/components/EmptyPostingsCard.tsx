import { Card } from "react-bootstrap"

export const EmptyPostingsCard = () => {

    return (
        <Card style={{ padding: 20 }}>
            <h3>No postings to display...</h3>
        </Card>
    )
}